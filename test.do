{
    let abs: int() = {
        il {
            store $x
        }

        x
        0
        if il { cmp > } {
            return -x
        } else {
            return x
        }

        for y in (for x in 0..7 { x + 1 }) {
            print y
        }

        let x: int[]

        let y = for v in x v + 8


        t

        while x < 1 {

        }
    }

    let x: int = il{read}
    print abs(x)
}

interface Iterator {

}